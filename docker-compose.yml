services:
  eventgo-web:
    build: .
    container_name: eventgo-production
    ports:
      - "80:80"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
